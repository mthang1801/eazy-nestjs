export enum ImageObjectType {
  LOGO = 'logos',
  PRODUCT = 'products',
  BANNER = 'banners',
  USER = 'users',
  CATEGORY = 'categories',
}

export enum ImageType {
  'Saved' = 'A',
  'Temporary_Save' = 'B',
}
